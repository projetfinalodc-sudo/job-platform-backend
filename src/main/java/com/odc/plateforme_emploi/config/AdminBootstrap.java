package com.odc.plateforme_emploi.config;

/**
 * AdminBootstrap désactivé.
 *
 * Avant, cette classe créait automatiquement un compte ADMIN
 * au premier démarrage de l'application.
 *
 * Cette fonctionnalité est maintenant supprimée.
 *
 * La création d'un administrateur doit se faire :
 * - via une action sécurisée d'un administrateur existant ;
 * - ou manuellement en base de données.
 */
public class AdminBootstrap {

    /**
     * Aucun traitement volontairement.
     *
     * Cette classe est conservée uniquement pour éviter de casser
     * la structure du projet.
     */
}